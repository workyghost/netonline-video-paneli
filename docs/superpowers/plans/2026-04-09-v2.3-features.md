# v2.3 Feature Set Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Orientation kaldırma, görsel (jpg/png) desteği, ekranlar listesi anlık güncelleme ve kalıcı error toast.

**Architecture:** Vanilla JS / HTML, Supabase SDK, XHR upload. Tüm değişiklikler `js/dashboard.js`, `js/player.js`, `player.html` dosyalarında. DB şeması değişmez — `orientation` kolonu kalır, kullanılmaz. Görsel algılama `file_name` uzantısından yapılır.

**Tech Stack:** Vanilla JS ES Modules, TailwindCSS CDN, Supabase JS v2, XHR upload, Playwright (test)

---

## Dosya Etki Listesi

| Dosya | Ne değişiyor |
|-------|-------------|
| `js/dashboard.js` | showToast (kalıcı error), orientation kaldır, fetchScreens parametre, image upload |
| `js/player.js` | orientation kaldır, playItem refactor, image playback |
| `player.html` | orientation radio kaldır, `<img id="mainImage">` / `<img id="bgImage">` ekle |
| `docs/CHANGELOG.md` | v2.3.1 girişi |
| `mock-server/server.js` | Değişmez |

---

## Task 1: Kalıcı Error Toast

**Files:**
- Modify: `js/dashboard.js:77-88`

- [ ] **Adım 1: showToast fonksiyonunu güncelle**

`js/dashboard.js` satır 77-88'i tamamen şununla değiştir:

```js
function showToast(message, type = "success") {
  const container = document.getElementById("toast-container");
  const colors = {
    success: "bg-green-600 text-white",
    error:   "bg-red-600 text-white",
    info:    "bg-gray-700 text-white"
  };
  const toast = document.createElement("div");
  toast.className = `flex items-start gap-2 px-4 py-3 rounded-lg text-sm font-medium shadow-lg pointer-events-auto ${colors[type] ?? colors.info}`;

  const msgSpan = document.createElement("span");
  msgSpan.className = "flex-1";
  msgSpan.textContent = message;
  toast.appendChild(msgSpan);

  const closeBtn = document.createElement("button");
  closeBtn.textContent = "✕";
  closeBtn.className = "ml-1 opacity-60 hover:opacity-100 flex-shrink-0 leading-none";
  closeBtn.addEventListener("click", () => toast.remove());
  toast.appendChild(closeBtn);

  container.appendChild(toast);

  if (type !== "error") {
    setTimeout(() => toast.remove(), 3500);
  }
}
```

- [ ] **Adım 2: Test et**

Mock server çalışıyorsa `http://localhost:3001/dashboard.html` aç:
- Başarılı bir işlem yap (örn: firma ekle) → yeşil toast 3.5s sonra kaybolur ✓
- Kasıtlı bir hata üret (geçersiz forma kaydet) → kırmızı toast ekranda kalır, ✕ butonuyla kapanır ✓

- [ ] **Adım 3: Commit**

```bash
git add js/dashboard.js
git commit -m "feat: error toast kalıcı, success toast 3.5s sonra kaybolur"
```

---

## Task 2: Orientation Kaldırma — dashboard.js

**Files:**
- Modify: `js/dashboard.js` (orientation sabitler, screens tablosu, contents filtresi, upload form)

- [ ] **Adım 1: ORIENTATION_LABEL ve ORIENTATION_BADGE sabitlerini sil**

Satır 138-139'u komple sil:
```js
// BU İKİ SATIRI SİL:
const ORIENTATION_LABEL = { horizontal: "Yatay", vertical: "Dikey", both: "Ortak" };
const ORIENTATION_BADGE = { horizontal: "bg-blue-500/10 text-blue-400", vertical: "bg-purple-500/10 text-purple-400", both: "bg-green-500/10 text-green-400" };
```

- [ ] **Adım 2: Screens tablosundan Yön sütununu kaldır**

`initScreens` içindeki tablo thead'inden "Yön" th'sini sil:
```html
<!-- SİL: -->
<th class="text-left px-4 py-3 text-xs text-gray-500 font-medium">Yön</th>
```

Screens tbody render'dan orientation td'sini sil:
```js
// SİL:
<td class="px-4 py-3 text-gray-400 text-xs">${esc(ORIENTATION_LABEL[s.orientation] || s.orientation)}</td>
```

`btn-edit-screen` butonundaki `data-orientation="${esc(s.orientation)}"` attribute'unu sil.

- [ ] **Adım 3: openAddScreenModal'dan Yön alanını kaldır**

Modal HTML'inden şu bloğu tamamen sil:
```html
<!-- BU BLOĞU SİL: -->
<div>
  <label class="block text-xs text-gray-400 mb-2">Yön</label>
  <div class="flex gap-4">
    <label class="flex items-center gap-2 cursor-pointer">
      <input type="radio" name="ms-orientation" value="horizontal" class="accent-blue-500"> <span class="text-sm text-gray-300">Yatay</span>
    </label>
    <label class="flex items-center gap-2 cursor-pointer">
      <input type="radio" name="ms-orientation" value="vertical" class="accent-blue-500"> <span class="text-sm text-gray-300">Dikey</span>
    </label>
  </div>
</div>
```

Save handler'dan orientation satırlarını sil/güncelle:
```js
// ESKİ (silindi):
const orientation = document.querySelector('input[name="ms-orientation"]:checked')?.value;
if (!firmId || !name || !location || !orientation) {

// YENİ:
if (!firmId || !name || !location) {
```

Insert'ten `orientation` kaldır:
```js
// ESKİ:
firm_id: firmId, name, location, orientation,
// YENİ:
firm_id: firmId, name, location,
```

- [ ] **Adım 4: openEditScreenModal'dan Yön alanını kaldır**

Fonksiyon imzasını güncelle:
```js
// ESKİ:
function openEditScreenModal({ id, name, location, orientation, firm }) {
// YENİ:
function openEditScreenModal({ id, name, location, firm }) {
```

Modal HTML'inden Yön bloğunu sil (add modal'daki gibi, `es-orientation` name'li radiolari).

Save handler'dan orientation satırlarını sil:
```js
// ESKİ:
const newOrient = document.querySelector('input[name="es-orientation"]:checked')?.value;
const { error } = await supabase.from("screens").update({ name: newName, location: newLocation, orientation: newOrient }).eq("id", id);
// YENİ:
const { error } = await supabase.from("screens").update({ name: newName, location: newLocation }).eq("id", id);
```

- [ ] **Adım 5: Contents sayfasından orientation filtresini kaldır**

`initContents` HTML'inden filter-orientation select'i sil:
```html
<!-- BU BLOĞU SİL: -->
<select id="filter-orientation" class="bg-gray-800 border border-gray-700 text-gray-300 text-sm rounded-lg px-3 py-1.5">
  <option value="">Tüm Yönler</option>
  <option value="horizontal">Yatay</option>
  <option value="vertical">Dikey</option>
  <option value="both">Ortak</option>
</select>
```

Contents thead'den "Yön" th'sini sil:
```html
<!-- SİL: -->
<th class="text-left px-4 py-3 text-xs text-gray-500 font-medium">Yön</th>
```

`renderVideos` içinden orientation filtresi satırlarını sil:
```js
// SİL:
const orientFilter = document.getElementById("filter-orientation")?.value || "";
// SİL:
if (orientFilter && v.orientation !== orientFilter) return false;
```

Contents tbody render'dan orientation td'sini sil:
```js
// SİL:
<td class="px-4 py-3"><span class="px-2 py-0.5 rounded text-xs font-medium ${ORIENTATION_BADGE[v.orientation] || ""}">${esc(ORIENTATION_LABEL[v.orientation] || v.orientation)}</span></td>
```

`forEach` event listener listesinden `"filter-orientation"` id'sini kaldır:
```js
// ESKİ:
["filter-firm", "filter-orientation"].forEach(id => {
// YENİ:
["filter-firm"].forEach(id => {
```

- [ ] **Adım 6: Upload formundan Yön select'i kaldır**

Upload modal HTML'inden orientation select div'ini sil:
```html
<!-- BU DIV'İ SİL: -->
<div>
  <label class="block text-xs text-gray-400 mb-1">Yön</label>
  <select id="upload-orientation" ...>
    <option value="">Seçin...</option>
    <option value="horizontal">Yatay</option>
    <option value="vertical">Dikey</option>
    <option value="both">Ortak</option>
  </select>
</div>
```

Upload submit handler'dan orientation satırlarını sil/güncelle:
```js
// SİL:
const orientation = document.getElementById("upload-orientation").value;
// SİL:
if (!orientation) { showToast("Yön seçin", "error"); return; }
```

`uploadSingleVideo` çağrısını güncelle:
```js
// ESKİ:
await uploadSingleVideo(file, title, firmId, orientation, expiry);
// YENİ:
await uploadSingleVideo(file, title, firmId, expiry);
```

`uploadSingleVideo` imzasını güncelle:
```js
// ESKİ:
function uploadSingleVideo(file, title, firmId, orientation, expiry) {
// YENİ:
function uploadSingleVideo(file, title, firmId, expiry) {
```

DB insert'ten `orientation` kaldır:
```js
// ESKİ:
title, firm_id: firmId, orientation, file_name: fileName, ...
// YENİ:
title, firm_id: firmId, file_name: fileName, ...
```

- [ ] **Adım 7: Test et**

Mock server açık, `http://localhost:3001/dashboard.html`:
- Ekranlar sekmesi: Yön sütunu yok ✓
- Yeni ekran ekle modalı: Yön radio yok, sadece firma/ad/konum ✓
- İçerikler sekmesi: Yön filtresi ve badge sütunu yok ✓
- Video yükle modal: Yön select yok ✓

- [ ] **Adım 8: Commit**

```bash
git add js/dashboard.js
git commit -m "feat: orientation (yön) alanı dashboard'dan kaldırıldı"
```

---

## Task 3: Orientation Kaldırma — player.html + player.js

**Files:**
- Modify: `player.html:54-67`
- Modify: `js/player.js` (orientation state/validation/filter)

- [ ] **Adım 1: player.html'den Yön alanını kaldır**

`player.html` satır 54-67 arasındaki tüm "Yön" bloğunu sil:
```html
<!-- BU BLOĞU SİL: -->
<!-- Yön -->
<div>
  <span class="block text-sm font-medium text-slate-300 mb-2">Ekran Yönü</span>
  <div class="flex gap-6">
    <label class="flex items-center gap-2 cursor-pointer">
      <input type="radio" name="screenOrientation" value="horizontal" class="accent-blue-500 w-4 h-4">
      <span class="text-slate-300 text-sm">Yatay</span>
    </label>
    <label class="flex items-center gap-2 cursor-pointer">
      <input type="radio" name="screenOrientation" value="vertical" class="accent-blue-500 w-4 h-4">
      <span class="text-slate-300 text-sm">Dikey</span>
    </label>
  </div>
</div>
```

- [ ] **Adım 2: player.js state'den orientation kaldır**

State bölümünden şu satırı sil:
```js
// SİL:
let currentOrientation = null;
```

- [ ] **Adım 3: saveScreenButton handler'ı güncelle**

```js
// ESKİ:
const orientationEl = document.querySelector('input[name="screenOrientation"]:checked');
const orientation = orientationEl?.value;

if (!firmId)      { showSetupError("Lütfen firma seçin.");     return; }
if (!name)        { showSetupError("Lütfen ekran adı girin."); return; }
if (!location)    { showSetupError("Lütfen konum girin.");     return; }
if (!orientation) { showSetupError("Lütfen yön seçin.");      return; }

// YENİ:
if (!firmId)   { showSetupError("Lütfen firma seçin.");     return; }
if (!name)     { showSetupError("Lütfen ekran adı girin."); return; }
if (!location) { showSetupError("Lütfen konum girin.");     return; }
```

DB insert'ten `orientation` kaldır:
```js
// ESKİ:
firm_id: firmId, name, location, orientation, status: "online", ...
// YENİ:
firm_id: firmId, name, location, status: "online", ...
```

- [ ] **Adım 4: startFirmMode'dan orientation kaldır**

```js
// ESKİ:
function startFirmMode(firmId, orientation) {
  if (currentFirmId === firmId && currentOrientation === orientation && currentPlaylistId === null) return;
  currentFirmId      = firmId;
  currentOrientation = orientation;
  currentPlaylistId  = null;

// YENİ:
function startFirmMode(firmId) {
  if (currentFirmId === firmId && currentPlaylistId === null) return;
  currentFirmId     = firmId;
  currentPlaylistId = null;
```

`fetchFirmVideos` içindeki orientation filtresini sil:
```js
// ESKİ:
const validVideos = (videos || [])
  .filter(v => !v.expires_at || new Date(v.expires_at) > now)
  .filter(v => v.orientation === orientation || v.orientation === "both");

// YENİ:
const validVideos = (videos || [])
  .filter(v => !v.expires_at || new Date(v.expires_at) > now);
```

- [ ] **Adım 5: startFirmMode çağrılarını güncelle**

`startPlayback` içindeki fetchScreen callback'inde:
```js
// ESKİ:
startFirmMode(screen.firm_id, screen.orientation);
// YENİ:
startFirmMode(screen.firm_id);
```

Link modu fetchScreen callback'inde (init fonksiyonu içindeki `fetchScreen`):
```js
// ESKİ:
startFirmMode(screen.firm_id, screen.orientation);
// YENİ:
startFirmMode(screen.firm_id);
```

- [ ] **Adım 6: Test et**

Mock server açık, `http://localhost:3001/player.html`:
- Setup formu: Firma / Ekran Adı / Konum var, Yön radio YOK ✓
- Kaydet butonuna tıkla → ekran kaydedilir ✓

- [ ] **Adım 7: Commit**

```bash
git add player.html js/player.js
git commit -m "feat: orientation (yön) player'dan kaldırıldı"
```

---

## Task 4: Screens Listesi Anlık Güncelleme

**Files:**
- Modify: `js/dashboard.js` (initScreens, openAddScreenModal, openEditScreenModal)

- [ ] **Adım 1: initScreens'te çağrıları güncelle**

`initScreens` içindeki `"btn-add-screen"` click handler:
```js
// ESKİ:
document.getElementById("btn-add-screen").addEventListener("click", () => openAddScreenModal());
// YENİ:
document.getElementById("btn-add-screen").addEventListener("click", () => openAddScreenModal(fetchScreens));
```

`btn-edit-screen` click handler:
```js
// ESKİ:
btn.addEventListener("click", () => openEditScreenModal(btn.dataset));
// YENİ:
btn.addEventListener("click", () => openEditScreenModal(btn.dataset, fetchScreens));
```

- [ ] **Adım 2: openAddScreenModal imzasını ve başarı callback'ini güncelle**

```js
// ESKİ:
function openAddScreenModal() {
// YENİ:
function openAddScreenModal(onSuccess) {
```

Başarılı insert sonrası:
```js
// ESKİ:
closeModal();
showToast("Ekran eklendi");
// YENİ:
closeModal();
onSuccess();
showToast("Ekran eklendi");
```

- [ ] **Adım 3: openEditScreenModal imzasını ve başarı callback'ini güncelle**

```js
// ESKİ:
function openEditScreenModal({ id, name, location, firm }) {
// YENİ:
function openEditScreenModal({ id, name, location, firm }, onSuccess) {
```

Başarılı update sonrası:
```js
// ESKİ:
closeModal();
showToast("Ekran güncellendi");
// YENİ:
closeModal();
onSuccess();
showToast("Ekran güncellendi");
```

- [ ] **Adım 4: Test et**

Mock server açık, `http://localhost:3001/dashboard.html`:
- Ekranlar sekmesine git
- "Yeni Ekran Ekle" → formu doldur → Kaydet
- Modal kapanır ve liste **aynı anda** yeni ekranı gösterir (yenileme gerekmez) ✓

- [ ] **Adım 5: Commit**

```bash
git add js/dashboard.js
git commit -m "fix: ekran ekle/düzenle sonrası liste anlık güncelleniyor"
```

---

## Task 5: Görsel Upload Desteği (dashboard.js)

**Files:**
- Modify: `js/dashboard.js` (upload modal, uploadSingleVideo)

- [ ] **Adım 1: isImage helper ekle**

`dashboard.js`'de `ORIENTATION_LABEL` sabitlerinin kaldırıldığı yere (veya `formatDate` fonksiyonunun altına) şunu ekle:
```js
const isImage = fn => /\.(jpg|jpeg|png)$/i.test(fn || "");
```

- [ ] **Adım 2: Upload modalını güncelle**

Drop zone metnini güncelle:
```js
// ESKİ:
<p class="text-xs text-gray-600 mt-1">MP4 · Çoklu seçim desteklenir</p>
// YENİ:
<p class="text-xs text-gray-600 mt-1">MP4 · JPG · PNG · Çoklu seçim desteklenir</p>
```

File input `accept` attribute:
```html
<!-- ESKİ: -->
<input type="file" id="upload-file-input" accept=".mp4,video/mp4" multiple class="hidden">
<!-- YENİ: -->
<input type="file" id="upload-file-input" accept=".mp4,video/mp4,.jpg,.jpeg,.png,image/jpeg,image/png" multiple class="hidden">
```

"+ Video Yükle" butonunu güncelle:
```html
<!-- ESKİ: -->
<button id="btn-upload-video" ...>+ Video Yükle</button>
<!-- YENİ: -->
<button id="btn-upload-video" ...>+ İçerik Yükle</button>
```

İçerik başlığını güncelle:
```html
<!-- Upload modal başlığı ESKİ: -->
<h3 class="text-base font-semibold text-white mb-4">Video Yükle</h3>
<!-- YENİ: -->
<h3 class="text-base font-semibold text-white mb-4">İçerik Yükle</h3>
```

- [ ] **Adım 3: Dosya filtresi ve hata mesajını güncelle**

`handleFileSelect` içindeki filtre:
```js
// ESKİ:
const valid = Array.from(files).filter(f => f.type === "video/mp4");
if (!valid.length) { showToast("Yalnızca MP4 kabul edilir", "error"); return; }

// YENİ:
const valid = Array.from(files).filter(f =>
  f.type === "video/mp4" || f.type === "image/jpeg" || f.type === "image/png"
);
if (!valid.length) { showToast("Yalnızca MP4, JPG, PNG kabul edilir", "error"); return; }
```

- [ ] **Adım 4: uploadSingleVideo'da görsel kontrolü ekle**

`uploadSingleVideo` içinde, XHR `onload` başarı bloğunda `generateThumbnail` çağrısını şöyle güncelle:

```js
// ESKİ:
let thumbnailUrl = "";
try { thumbnailUrl = await generateThumbnail(file, fileName); } catch (_) {}

// YENİ:
let thumbnailUrl = "";
if (isImage(fileName)) {
  thumbnailUrl = fileUrl; // görselin kendisi thumbnail olarak kullanılır
} else {
  try { thumbnailUrl = await generateThumbnail(file, fileName); } catch (_) {}
}
```

- [ ] **Adım 5: Test et**

Mock server açık, `http://localhost:3001/dashboard.html`:
- İçerikler sekmesi → "İçerik Yükle" butonu ✓
- Modal'da "MP4 · JPG · PNG" yazısı ✓
- Masaüstünden bir JPG seç → forma firma seç → yükle → başarılı ✓
- Görsel içerikler listesinde thumbnail olarak görünür ✓
- Bir MP4 seç → yükle → başarılı ✓

- [ ] **Adım 6: Commit**

```bash
git add js/dashboard.js
git commit -m "feat: görsel (jpg/png) upload desteği eklendi"
```

---

## Task 6: Görsel Oynatma Desteği (player.html + player.js)

**Files:**
- Modify: `player.html:93-95` (img elementleri ekle)
- Modify: `js/player.js` (DOM refs, state, playItem refactor)

- [ ] **Adım 1: player.html'e img elementleri ekle**

`player.html`'de `<video id="mainVideo">` satırından **sonra** şunu ekle:
```html
<!-- Blur background image (görseller için) -->
<img id="bgImage"   class="absolute inset-0 w-full h-full object-cover blur-xl scale-110 opacity-60 hidden" alt="">
<!-- Main image -->
<img id="mainImage" class="absolute inset-0 w-full h-full object-contain z-10 hidden" alt="">
```

- [ ] **Adım 2: player.js'e DOM referansları ve state ekle**

Mevcut DOM refs bölümüne ekle (diğer const'lardan sonra):
```js
const bgImage   = document.getElementById("bgImage");
const mainImage = document.getElementById("mainImage");
```

State bölümüne ekle:
```js
let imageTimer = null;
```

`isImage` helper ekle (state'in altına):
```js
const isImage = fn => /\.(jpg|jpeg|png)$/i.test(fn || "");
```

- [ ] **Adım 3: playVideo → playItem olarak yeniden yaz**

Mevcut `playVideo` fonksiyonunu (satır 237-250) tamamen şununla değiştir:

```js
function playItem(index) {
  if (videoQueue.length === 0) return;
  if (imageTimer) { clearTimeout(imageTimer); imageTimer = null; }

  currentVideoIndex = index % videoQueue.length;
  currentVideo      = videoQueue[currentVideoIndex];

  if (isImage(currentVideo.file_name)) {
    // === GÖRSEL MODU ===
    mainVideo.pause();
    mainVideo.removeAttribute("src");
    bgVideo.pause();
    bgVideo.removeAttribute("src");
    mainVideo.classList.add("hidden");
    bgVideo.classList.add("hidden");

    mainImage.classList.remove("hidden");
    bgImage.classList.remove("hidden");
    mainImage.src = currentVideo.file_url;
    bgImage.src   = currentVideo.file_url;

    mainImage.onerror = () => {
      mainImage.onerror = null;
      consecutiveErrors++;
      if (consecutiveErrors >= 3) {
        showContentError();
        if (retryTimeout) clearTimeout(retryTimeout);
        retryTimeout = setTimeout(() => {
          consecutiveErrors = 0;
          hideContentError();
          if (videoQueue.length > 0) playItem(currentVideoIndex);
        }, 5 * 60 * 1000);
      } else {
        setTimeout(playNext, 3000);
      }
    };

    imageTimer = setTimeout(() => {
      consecutiveErrors = 0;
      playNext();
    }, 30000);

  } else {
    // === VİDEO MODU ===
    mainImage.classList.add("hidden");
    bgImage.classList.add("hidden");
    mainVideo.classList.remove("hidden");
    bgVideo.classList.remove("hidden");

    mainVideo.src = currentVideo.file_url;
    bgVideo.src   = currentVideo.file_url;
    mainVideo.load();
    bgVideo.load();
    mainVideo.muted = false;
    bgVideo.muted   = true;
    mainVideo.play().catch(console.error);
    bgVideo.play().catch(console.error);
  }
}
```

- [ ] **Adım 4: playNext, updateVideoQueue ve retry güncellemeleri**

`playNext` fonksiyonunu güncelle:
```js
// ESKİ:
function playNext() {
  playVideo(currentVideoIndex + 1);
}
// YENİ:
function playNext() {
  playItem(currentVideoIndex + 1);
}
```

`updateVideoQueue` içindeki `playVideo(0)` → `playItem(0)`:
```js
// ESKİ:
playVideo(0);
// YENİ:
playItem(0);
```

`mainVideo.addEventListener("error", ...)` içindeki retry:
```js
// ESKİ (satır 275):
if (videoQueue.length > 0) playVideo(currentVideoIndex);
// YENİ:
if (videoQueue.length > 0) playItem(currentVideoIndex);
```

`imageTimer` temizliği için `updateVideoQueue`'un başına ekle (videoQueue sıfırlandığında timer'ı da sıfırla):
```js
function updateVideoQueue(newVideos) {
  if (imageTimer) { clearTimeout(imageTimer); imageTimer = null; }  // ← EKLE
  if (newVideos.length === 0) {
    // ... mevcut kod
```

- [ ] **Adım 5: cleanupAndShowSetup'ta imageTimer temizle**

```js
function cleanupAndShowSetup() {
  if (heartbeatInterval) clearInterval(heartbeatInterval);
  if (imageTimer) { clearTimeout(imageTimer); imageTimer = null; }  // ← EKLE
  stopAllListeners();
  // ... mevcut kod
```

- [ ] **Adım 6: Görsel playlist testi**

Mock server açık. Önce bir JPG yükle (Task 5 ile), sonra:
1. Playlist oluştur, görseli ekle
2. Bir ekran oluştur, playlistini ata
3. Ekranın linkini kopyala → yeni sekmede aç
4. "Oynatmak için tıklayın" overlay'ine tıkla
5. Görsel tam ekran görünür, blur background ile birlikte ✓
6. 30 saniye sonra (veya playlist'te başka içerik varsa) geçer ✓

- [ ] **Adım 7: Commit**

```bash
git add player.html js/player.js
git commit -m "feat: görsel (jpg/png) oynatma desteği, 30s süre, blur background"
```

---

## Task 7: CHANGELOG ve Sürüm Güncelleme

**Files:**
- Modify: `docs/CHANGELOG.md`
- Modify: `CLAUDE.md` (sürüm numarası)

- [ ] **Adım 1: CHANGELOG.md'ye v2.3.1 ekle**

`docs/CHANGELOG.md` dosyasına en üste yeni sürüm girdisi ekle:

```markdown
## [2.3.1] — 2026-04-09

### Yeni Özellikler
- **Görsel desteği**: JPG/PNG içerik yüklenebilir ve player'da 30 saniye görüntülenir (blur background ile)
- **Kalıcı error toast**: Hata mesajları manuel kapatılana kadar ekranda kalır; başarı mesajları 3.5s sonra kaybolur

### İyileştirmeler
- **Orientation (Yön) kaldırıldı**: Tüm formlar, filtreler ve tablo sütunlarından yön alanı çıkarıldı; player tüm aktif içerikleri oynatır
- **Ekranlar listesi**: Yeni ekran eklenince/düzenlenince liste anında güncellenir (yenileme gerekmez)
```

- [ ] **Adım 2: CLAUDE.md sürüm numarasını güncelle**

`CLAUDE.md`'de `Mevcut sürüm: v2.2.9` → `Mevcut sürüm: v2.3.1`

- [ ] **Adım 3: Final commit**

```bash
git add docs/CHANGELOG.md CLAUDE.md
git commit -m "chore: v2.3.1 CHANGELOG ve sürüm güncelleme"
```

---

## Self-Review Notları

**Spec coverage kontrol:**
- ✅ Orientation kaldırma: Task 2 (dashboard) + Task 3 (player)
- ✅ Görsel upload: Task 5
- ✅ Görsel oynatma (30s, blur): Task 6
- ✅ Screens anlık güncelleme: Task 4
- ✅ Kalıcı error toast: Task 1
- ✅ Sürüm: Task 7

**Bağımlılık sırası:**
- Task 1 bağımsız
- Task 2 → Task 4 bağımlı (orientation kaldırıldıktan sonra fetchScreens refactor daha temiz)
- Task 3 bağımsız (player dosyaları)
- Task 5 → Task 6 bağımlı (isImage helper Task 5'te tanımlanır, Task 6'da player.js'de ayrıca tanımlanır — DRY değil ama farklı modüller, kabul edilebilir)
- Task 7 en son

**İmza tutarlılığı:**
- `playItem(index)` Task 6'da tanımlanır, `playNext()` ve `updateVideoQueue()` içinde kullanılır ✓
- `isImage(fn)` hem dashboard.js (Task 5) hem player.js (Task 6) için ayrı ayrı tanımlanır ✓
- `openAddScreenModal(onSuccess)` ve `openEditScreenModal(dataset, onSuccess)` Task 4'te tutarlı ✓
